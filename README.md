# company

Formatting and linting:

- `pnpm format` applies Oxfmt formatting.
- `pnpm format:check` checks formatting without changing files.
- `pnpm lint` (or `pnpm lint:ox`) runs Oxlint across the repository.
- GitHub Actions runs formatting and lint checks on pushes and pull requests using Node 24.
